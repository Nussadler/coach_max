// src/helpers/resolveWorkout.ts
// Kein Workout-Typ in plan.ts vorhanden, daher als Typ entfernen
// import { Workout } from "../data/plan";

// Dummy-Interface für Kompatibilität, falls benötigt
// type Workout = { type: string; [key: string]: any };

// Dummy-Defaults, bis workoutDefaults existiert oder entfernt werden kann
// const workoutDefaults: Record<string, Partial<Workout>> = {};

// export function resolveWorkout(workout: Workout): Workout {
//   const defaults = workoutDefaults[workout.type] || {};
//   return { ...defaults, ...workout };
// }

// Funktion entfernt, da kein Workout-Typ existiert
